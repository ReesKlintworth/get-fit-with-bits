import { StyleSheet } from 'react-native';
import Colors from '../../Colors';

export default StyleSheet.create({
  flatlist: {
    paddingLeft: 16,
    backgroundColor: 'transparent',
  },
  separator: {
    height: 1,
    backgroundColor: Colors.leDivider,
  },
});
